apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: use-librenms-scc
rules:
- apiGroups: ["security.openshift.io"]
  resources: ["securitycontextconstraints"]
  resourceNames: ["librenms-netadmin"]
  verbs: ["use"]
