apiVersion: cdi.kubevirt.io/v1beta1
kind: DataVolume
metadata:
  name: maas-vm-rootdisk
  annotations:
    argocd.argoproj.io/sync-options: Delete=False
spec:
  source:
    pvc:
      namespace: infrastructure-vms
      name: ubuntu24-04-golden-image
  pvc:
    accessModes:
      - ReadWriteMany
    resources:
      requests:
        storage: 200Gi
