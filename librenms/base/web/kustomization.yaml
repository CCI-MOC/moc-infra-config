apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - includeSelectors: true
    pairs:
      component: web
resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - route.yaml
